pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

plugins {
    alias(libs.plugins.android.application) apply false
    alias(libs.plugins.kotlin.android) apply false
    # Удаляем alias(libs.plugins.kotlin.compose)
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}